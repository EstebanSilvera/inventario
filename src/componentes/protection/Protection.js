

























































































































































































































import { Navigate } from "react-router-dom";


export const Protection = ({ children }) => {
    
    const authentication = sessionStorage.getItem("TOKEN");
    const user = sessionStorage.getItem("USER");

    const first = JSON.parse(localStorage.getItem("@DMIÑ"))
    
    const tokenFirst = authentication?.substring(0,21)
        
    if (tokenFirst === first?.first && authentication?.substring(21,) === first?.second && user === "administrador" ) {
      return children
    }
      
    return <Navigate to="/" />
  }